name: Build Tailwind CSS

on:
  push:
    branches: [main]
    paths:
      - "**/*.html"
      - "src/tailwind.css"
      - "public/css/**/*.css"
      - "package.json"
      - ".github/workflows/build-tailwind.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm run build:css
      - name: Verify Tailwind migration
        run: |
          if grep -RniE 'bootstrap(-rtl)?\.css|cdn\.jsdelivr\.net/npm/@tailwindcss/browser' --include='*.html' .; then
            echo "Legacy Bootstrap CSS or Tailwind Play CDN reference found"
            exit 1
          fi
          if grep -RniE '<link[^>]+bootstrap' --include='*.html' .; then
            echo "Bootstrap stylesheet reference found"
            exit 1
          fi
      - name: Commit generated CSS
        run: |
          git add -f public/css/tailwind.css
          if git diff --cached --quiet -- public/css/tailwind.css; then
            echo "No CSS changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/css/tailwind.css
          git commit -m "build: regenerate Tailwind CSS"
          git push origin HEAD:main
